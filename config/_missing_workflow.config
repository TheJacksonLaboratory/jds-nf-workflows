// This file is loaded only when the requested workflow config is missing.
params.fallback_config_loaded = true
params.requested_config_path = params.config ?: "config/${params.workflow ?: params.pipeline}.config"

throw new IllegalArgumentException(
    "Requested workflow config was not found: ${params.requested_config_path}"
)